<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product List</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h1 class="display-5 text-center my-4">Product List</h1>
        <%
            if(products.length > 0){
        %>
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>Image</th>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Category</th>
                    <th>Price</th>
                    <th>Qty</th>
                    <th>Created</th>
                    <th>Updated</th>
                    <th>Manage</th>
                </tr>
            </thead>
            <tbody>
                <%
                    for(let row=0; row < products.length; row++)
                    {
                %>
                    <tr>
                        <td><img src="<%=products[row].ProductPicture%>" width="50"></td>
                        <td><%=products[row].ProductID%></td>
                        <td><%=products[row].ProductName%></td>
                        <td><%=products[row].CategoryID%></td>
                        <td><%=products[row].UnitPrice%></td>
                        <td><%=products[row].UnitInStock%></td>
                        <td><%=products[row].CreatedDate%></td>
                        <td><%=products[row].ModifiedDate%></td>
                        <td style="width: 150px;">
                            <a href="#" class="btn btn-sm btn-warning">Edit</a> 
                            <a href="#" class="btn btn-sm btn-danger">Delete</a> 
                        </td>
                    </tr>
                <%
                    }
                %>
            </tbody>
        </table>     
        <%
            }
        %>
    </div>
</body>
</html>